[package]
name = "lastconvoy"
version = "0.5.1"
edition = "2024"
license = "MIT"

[features]
debug-log = []

[dependencies]
macroquad = "~0.4"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"
quad-storage = "0.1.3"
