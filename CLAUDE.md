@AGENTS.md

User preference (2026-02-28): default to Monogram bitmap font assets (`assets/fonts/monogram-bitmap.png` + `assets/fonts/monogram-bitmap.json`), with the same JSON used for both regular and italic bitmap variants.
